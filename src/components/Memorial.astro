---
import { site, emph } from "../site";
const m = site.home.memorial;
---
<section class="keller">
  <div class="wrap">
    <span class="lab">{m.kicker}</span>
    <h3 class="disp rv" set:html={emph(m.name)} />
    <div class="yr rv">{m.years}</div>
    <p class="rv">{m.body}</p>
  </div>
</section>
